# Requirements Status

Tracked from client PRs #23-#26 and `REQS.md`. Last updated: 2026-03-23.

## P0 (Critical)

| ID | Title | Status | Notes |
|----|-------|--------|-------|
| 1.0 | Unified Component Lifecycle | Done | `@dsl.container_component`, `cli()→execute()→run()`, `BaseComponent` |
| 5.0 | Airflow DAG 403 Permission Denial | Done | Composer SA granted `aiplatform.user` + SA impersonation |
| 6.0 | Compiled YAML embeds Python source | Done | Resolved by 1.0 — container components, no inlined code |
| 7.0 | Pydantic migration | Done | All components use `BaseSettings`/`BaseModel`, Pydantic v2 |
| 8.0 | Replace argparse with Typer | Done | `BaseComponent.cli()` auto-generates flags from Pydantic fields |
| 11.0 | Simplify PipelineBuilder API | Done | Single `.add()` method, `Pipeline` builder with `.build()` |
| 12.0 | Refactor CLI entrypoints | Done | Resolved by 1.0 — `python -m <step_module>` via `as_kfp_component()` |
| 13.0 | Flatten ComponentConfig into BaseComponent | Done | `machine_type`, `accelerator_type`, etc. are direct fields on `BaseComponent` |
| 14.0 | Expose standard variables in base components | Done | `project`, `region`, `environment`, `branch`, `dataset` on every component |
| 15.0 | Rename GitState to Environment | Done | `Environment` enum (`local`, `dev`, `staging`, `prod`), `ENVIRONMENT` env var |
| 21.0 | Model Registry step | Done | `RegisterModel` with versioning via `parent_model`, serving image ownership |
| 22.0 | Loop and conditional operators | Done | `Pipeline.for_each()` → `dsl.ParallelFor`, `Pipeline.condition()` → `dsl.If`/`dsl.Else` |

## P1 (Important)

| ID | Title | Status | Notes |
|----|-------|--------|-------|
| 2.0 | Project name underscore normalization | Done | `_slugify()` in `naming.py`, hyphens for GCP resources |
| 3.0 | Clean out invalid DAGs | Done | `dags/` is gitignored, regenerated by `gml compile` |
| 4.0 | Docker build tag — always branch-sha | Done | `NamingConvention.image_tag()` returns `{branch}-{sha}`, never `:latest` |
| 9.0 | Structured logging with loguru | Done | All components use `loguru.logger`, no `print()` or stdlib logging |
| 16.0 | Separation of CI/CD and framework concerns | Done | `ENVIRONMENT` is an env var (CI/CD sets it), not derived from git. `get_git_branch()` only for naming. |
| 18.0 | Docker Build Cloud migration | Done | `gml build` wraps `gcloud builds submit`, `cloudbuild.yaml` with 3-tier hierarchy |
| 18.1 | Simplify Docker image hierarchy | Done | `base-python` → `{pipeline}--base` → `{pipeline}--serve` per pipeline |

## P2 (Nice to Have)

| ID | Title | Status | Notes |
|----|-------|--------|-------|
| 10.0 | Google-style docstrings | Partial | Key classes documented, not exhaustive across all methods |
| 17.0 | Enforce mypy annotations | Done | `mypy gcp_ml_framework/` passes with zero errors, 100% annotations |

## Other

| ID | Title | Status | Notes |
|----|-------|--------|-------|
| 19.0 | DBT integration verification | Done | `DBTRun` component with `render_operator()` (BashOperator), used in verification_pipeline |
| 20.0 | AGENTS.md for AI-assisted coding | Done | `AGENTS.md` + `CLAUDE.md` in repo root |

## Summary

| Priority | Total | Done | Partial | Not Started |
|----------|-------|------|---------|-------------|
| P0 | 12 | 12 | 0 | 0 |
| P1 | 7 | 7 | 0 | 0 |
| P2 | 2 | 1 | 1 | 0 |
| Other | 2 | 2 | 0 | 0 |
| **Total** | **23** | **22** | **1** | **0** |
