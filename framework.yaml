team: mlplatform

# TODO: enforce project name is same as repo name or folder name 
project: second_run 

gcp:
  dev_project_id: prj-0n-dta-pt-ai-sandbox
  # TODO: deploy was using staging project id
  staging_project_id: prj-0n-dta-pt-ai-sandbox
  prod_project_id: prj-0n-dta-pt-ai-sandbox-prod
  region: us-east4
  composer_dags_path:
    dev: "gs://us-east4-mlopshousingpoc-030c6745-bucket/dags"
    # dev: "gs://composer-gap-0n-dta-pt-ai-sbx/dags"
    staging: ""
    prod: ""
  artifact_registry_host: us-east4-docker.pkg.dev

secrets:
  secret_prefix:             # defaults to namespace token; override only if needed

