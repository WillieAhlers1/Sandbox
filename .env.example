# GCP ML Framework — project config
# This file is gitignored — never commit real values.

# --- Identity (required) ---
TEAM=mlplatform
PROJECT=****
ENVIRONMENT=local
BRANCH=***

# --- GCP Project IDs ---
GCP_PROJECT_ID=****
GCP_REGION=us-east4

# --- Artifact Registry ---
# THESE VARIABLES SHOULD NOT BE REQUIRED. THEY SHOULD BE DERIVED FROM TEAM AND PROJECT
GCP_AR_HOST=us-east4-docker.pkg.dev
GCP_AR_REPO=mlplatform-third-run
GCP_COMPOSER_DAGS_PATH=****
GCP_PIPELINE_SERVICE_ACCOUNT_EMAIL=****

# --- Cloud Composer (fill after Terraform provisions) ---
# GML_GCP__COMPOSER_ENVIRONMENT_NAME=
# GML_GCP__COMPOSER_DAGS_PATH__DEV=gs://composer-bucket/dags
